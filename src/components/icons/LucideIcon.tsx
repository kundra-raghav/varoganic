import type { ReactElement } from 'react'

/**
 * Lightweight wrapper to standardize Lucide icons with sizing and color tokens.
 */
export type LucideIconProps = {
  readonly name: string
}

export const LucideIcon = (_props: LucideIconProps): ReactElement | null => {
  void _props
  return null
}
