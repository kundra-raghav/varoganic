import type { ReactElement } from 'react'

/**
 * Breadcrumb trail component for hierarchical navigation.
 */
export type BreadcrumbsProps = Record<string, never>

export const Breadcrumbs = (_props: BreadcrumbsProps): ReactElement | null => {
  void _props
  return null
}
