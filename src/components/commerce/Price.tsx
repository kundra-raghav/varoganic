import type { ReactElement } from 'react'

/**
 * Price display component with compare-at support.
 */
export type PriceProps = Record<string, never>

export const Price = (_props: PriceProps): ReactElement | null => {
  void _props
  return null
}
